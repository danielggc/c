#include<stdio.h>
 class  SumaEdad {
 public:
  int edades(int x;  );
 private:
  int resultado;
};
int SumaEdad::edades(int x; ){
  resultado=x+5;
   return resultado;

 }

int main(){
  int edad =4;
  SumaEdad suma ;
  printf(" la suma de la edad de x y y es %d",suma.edades(edad ) );
  

 
 } 
